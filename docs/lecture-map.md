# Lecture Map

| Repository demonstration | Slide key |
| --- | --- |
| Cross-user booking evidence | `incident-cold-open` through `incident-uncertainty` |
| Scoped Eloquent query and policy | `secure-scoped-query`, `secure-framework-anchors` |
| Two-user feature test | `testing-security-behavior`, `testing-two-user-example` |
| CI tests and scanner gates | `ci-pull-request-evidence` through `ci-scanner-limits` |
| Expensive denied-request evidence | `api-outage-cold-open` through `api-outage-reveal` |
| Nginx, rate limit, and cheap rejection | `api-outage-controls` |
| Structured security events | `observability-security-event` |
| Credential-safe logging | `observability-second-leak` |
| Request IDs across evidence | `observability-correlation` |
| Pinned GitHub Actions and dependency review | `ai-supply-chain`, `ai-trust-model` |

The repository intentionally demonstrates that generic scanners cannot prove
object ownership. The two-user feature test expresses that product rule.
